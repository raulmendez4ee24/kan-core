[pytest]
# Ensure repo root packages (brain/, routes/, etc.) are importable when running
# `pytest` via the entrypoint script (sys.path[0] is not the repo root in that case).
pythonpath = .
testpaths = tests
filterwarnings =
    ignore:.*coroutine 'Connection\._cancel' was never awaited.*:RuntimeWarning
